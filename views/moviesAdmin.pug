doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link( rel="stylesheet", href="/styles.css")
        title Mis películas
    body 
        main
            
            h1.title Mis películas
            a(href="/admin/createMovie") 
                button.btn Crear nueva
            section.movies_container 
                div.popup 
                    button.close 
                    h3 ¿Estas seguro que quieres eliminar esta película?
                    a#confirm_del(href="") Eliminar
                each movie in movies
                    section.movie_cont
                        span 
                            a(href='/admin/editMovie/'+ movie.id ) 
                                button.btn Editar
                            button.btn.btn_delete(type="submit" name=movie.id) Eliminar
                        img(crossorigin="anonymous" src=movie.image, alt="", srcset="")
                        h3 Título: #{movie.fullTitle}
                        p Director: #{movie.director} 
                        p Año de lanzamiento: #{movie.year} 
                        p Plot: 
                        p #{movie.plot} 
                        p Duración: #{movie.runtimeStr} 
        script(src="/delete.js" type='module') 


